# Only include constants here that are actually static and not
# configurable. Constants that should be configurable and are
# constant for an entire application session reside in `app/consts.py`

APPLICATION_LOGGER_NAME = "prometheus.api"
